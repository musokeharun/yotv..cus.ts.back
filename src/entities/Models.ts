import ParseServer from "../sources/ParseServer";

export const Users = ParseServer.Object.extend("Users");
export const Bulk = ParseServer.Object.extend("Bulk");
export const Contact = ParseServer.Object.extend("Contact");